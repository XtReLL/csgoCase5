type User {
  id: ID
  username: String
  steamId: String
  avatar: String
  trade_url: String
  balance: Int
}

type Query {
  user(id: ID): User!
}

type Mutation {
  setTradeLink(link: String): Boolean
}