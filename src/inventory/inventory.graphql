type Inventory {
  id: ID!
  userId: Int
  itemId: Int
  price: Int
  status: Int
}

enum InventoryStatus {
  AVAILABLE
}

type InventoryListData {
  data: [Inventory]!
  pagination: CursorBasedPaginationData!
}

type Mutation {
  sellItem(id: ID!): Boolean!
}
